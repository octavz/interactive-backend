package com.wantsome

package interactive

import io.circe._, io.circe.generic.semiauto._

import commons.db._
import commons.models.User

trait JsonSupport {
  /*

  implicit val idDecoder: Decoder[Id] = deriveDecoder[Id]
  implicit val idEncoder: Encoder[Id] = deriveEncoder[Id]

  implicit val idcDecoder: Decoder[ComboId] = deriveDecoder[ComboId]
  implicit val idcEncoder: Encoder[ComboId] = deriveEncoder[ComboId]

  implicit val userDecoder: Decoder[User] = deriveDecoder[User]
  implicit val userEncoder: Encoder[User] = deriveEncoder[User]
 */
}
